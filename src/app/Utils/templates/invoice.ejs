

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Booking Invoice</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f4f6f9;
      margin: 0;
      padding: 0;
      color: #333;
    }
    .container {
      max-width: 800px;
      margin: 40px auto;
      background: #ffffff;
      border-radius: 12px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
      overflow: hidden;
    }
    .header {
      background: linear-gradient(135deg, #1e3a8a, #3b82f6);
      color: white;
      padding: 40px 30px;
      text-align: center;
    }
    .header h1 {
      margin: 0;
      font-size: 2.5rem;
      font-weight: 600;
    }
    .header p {
      margin: 10px 0 0;
      font-size: 1.1rem;
      opacity: 0.9;
    }
    .content {
      padding: 40px;
    }
    .invoice-details {
      display: flex;
      justify-content: space-between;
      flex-wrap: wrap;
      gap: 20px;
      margin-bottom: 40px;
      font-size: 1rem;
    }
    .detail-group {
      flex: 1;
      min-width: 250px;
    }
    .detail-group h3 {
      margin: 0 0 12px;
      color: #1e40af;
      font-size: 1.1rem;
      border-bottom: 2px solid #e5e7eb;
      padding-bottom: 8px;
    }
    .summary-table {
      width: 100%;
      border-collapse: collapse;
      margin: 30px 0;
      background: #f8fafc;
    }
    .summary-table th,
    .summary-table td {
      padding: 16px 20px;
      text-align: left;
      border-bottom: 1px solid #e2e8f0;
    }
    .summary-table th {
      background: #eef2ff;
      color: #1e40af;
      font-weight: 600;
    }
    .summary-table .amount {
      text-align: right;
      font-size: 1.4rem;
      font-weight: 700;
      color: #1e3a8a;
    }
    .footer {
      text-align: center;
      padding: 30px;
      background: #f8fafc;
      color: #64748b;
      font-size: 1rem;
    }
    .footer strong {
      color: #1e40af;
    }
    @media (max-width: 600px) {
      .invoice-details {
        flex-direction: column;
      }
      .header h1 {
        font-size: 2rem;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <!-- Header -->
    <div class="header">
      <h1>Booking Invoice</h1>
      <p>Thank you for your booking!</p>
    </div>

    <!-- Main Content -->
    <div class="content">
      <!-- Invoice & Customer Details -->
      <div class="invoice-details">
        <div class="detail-group">
          <h3>Booking Information</h3>
          <p><strong>Tour:</strong> <%= tourTitle %></p>
          <p><strong>Booking Date:</strong> <%= bookingDate %></p>
          <p><strong>Guests:</strong> <%= guestCount %> Person<%= guestCount > 1 ? 's' : '' %></p>
          <p><strong>Transaction ID:</strong> <%= transactionId %></p>
        </div>

        <div class="detail-group">
          <h3>Customer Details</h3>
          <p><strong>Name:</strong> <%= userName %></p>
        </div>
      </div>

      <!-- Payment Summary Table -->
      <table class="summary-table">
        <thead>
          <tr>
            <th>Description</th>
            <th>Amount</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>Tour Package: <strong><%= tourTitle %></strong> (<%= guestCount %> Guest<%= guestCount > 1 ? 's' : '' %>)</td>
            <td class="amount">৳<%= totalAmount.toLocaleString() %></td>
          </tr>
          <tr>
            <td style="font-weight: 600; color: #1e40af;">Total Paid</td>
            <td class="amount">৳<%= totalAmount.toLocaleString() %></td>
          </tr>
        </tbody>
      </table>

      <!-- Thank You Message -->
      <div class="footer">
        <p>We’re excited to have you on this adventure!</p>
        <p>If you have any questions, feel free to contact us.</p>
      </div>
    </div>
  </div>
</body>
</html>








<!-- 

 <div class="container">
        <h2>Invoice</h2>
        <h3><%= bookingDate %></h6>
        <h3><%= guestCount %></h6>
        <h3><%= transactionId %></h6>
        <h3><%= tourTitle %></h6>
        <h3><%= totalAmount %></h6>
        <h3><%= userName %></h6>
        <p>Thank you for booking</p>
</div> -->